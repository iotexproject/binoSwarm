{
    "name": "@elizaos/plugin-icn",
    "version": "0.1.0",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@elizaos/core": "workspace:*",
        "axios": "^1.7.9",
        "tsup": "8.3.5"
    },
    "scripts": {
        "build": "tsup --format esm --dts",
        "dev": "tsup --format esm --dts --watch",
        "test": "vitest run",
        "lint": "eslint --fix  --cache .",
        "test:coverage": "vitest run --coverage",
        "test:watch": "vitest"
    },
    "peerDependencies": {
        "whatwg-url": "7.1.0"
    }
}
